let teas = ["masala", "ginger", "oolong"]
let i = 9;
for (let i = 0; i < teas.length + 1; ++i) {
// for (let i = 0; i < teas.length; ) {
    // console.log(`Tea at index ${i}: teas[${i}]`);
    console.log(`Tea at index ${i}: ${teas[i]}`);
    
}